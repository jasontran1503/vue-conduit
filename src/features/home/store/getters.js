const getters = {
  articles: (state) => state.articles,
  tags: (state) => state.tags,
  articlesStatus: (state) => state.articlesStatus,
  tagsStatus: (state) => state.tagsStatus
};

export default getters;
